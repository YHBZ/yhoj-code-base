// The code here will be written privately.
// We won't open the source in this file.
async function email_verify(email: string): Promise<Boolean> {
    /**
     * TODO:
     * 1. search from database
     * 2. generate temp link
     * 3. send validation email to provided email
     * 4. return true
     */
    return true;
}
export default email_verify;